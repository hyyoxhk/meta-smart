slot=`otagent -g`

if [ x$slot == "xB" ]; then
	selection="-e stable,copyA"
elif [ x$slot == "xA" ]
	selection="-e stable,copyB"
else
    selection="--version"
fi

SWUPDATE_EXTRA_ARGS="${selection} -f /etc/swupdate.cfg"
